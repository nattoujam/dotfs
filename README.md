# dotfs
## Usage
### bash
```
git clone https://github.com/nattoujam/dotfs.git
./dotfs/zsh/init.sh
```

### zsh
```
sudo apt install zsh
chsh -s `which zsh`
git clone https://github.com/nattoujam/dotfs.git
./dotfs/zsh/init.sh
zsh
zplug install
```
